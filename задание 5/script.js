 function rgb(a,b,c) {
  if ( !isNaN(a) && !isNaN(b) && !isNaN(c) && a!= null && b!= null && c!= null ){
   a += `, `;
   b += `, `;
  return sum = `rgb `+ a + b + c ;
  } return (`Введите число`)
 }

console.log (rgb (23, 231 ,2));
